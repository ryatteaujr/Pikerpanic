{
  "name": "warehouse-picker-panic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve-dist.mjs",
    "serve": "node scripts/serve-dist.mjs",
    "vite": "vite --host 127.0.0.1 --port 5173",
    "build": "tsc && vite build --base ./",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
